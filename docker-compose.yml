version: "3.9"

services:
  server:
    build: ./server
    ports:
      - "4000:4000"

  web:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./web:/app
    command: sh -c "npm install && npm run dev -- --host"
    ports:
      - "5173:5173"
    depends_on:
      - server
